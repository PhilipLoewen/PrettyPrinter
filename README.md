The PrettyPrinter module offers just one function, `ppm`.
That's an initialism for "pretty-print matrix".
To see this function at work, run the Python script `test-ppm.py`.
It's supposed to be self-explanatory.
